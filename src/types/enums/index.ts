export enum ROLE_ENUM {
  ADMIN = 'admin',
  USER = 'user',
  MERCHANT = 'merchant',
  SELLER = 'seller',
  CUSTOMER = 'customer',
  SUPPLIER = 'supplier',
  DELIVERY = 'delivery',
  HR = 'hr',
  ACCOUNTANT = 'accountant',
}

export enum PLATFORM_ENUM {
  LOCAL = 'local',
  GOOGLE = 'google',
}
